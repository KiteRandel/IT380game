{
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",   
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "three": "^0.168.0",
    "vite": "^5.4.4"
  },
  "devDependencies": {
    "gh-pages": "^5.0.0" 
  }
}
