{
  "scripts": {
    "build": "vite build",
    "predeploy": "npm run build",
    "deploy": "pages -d dist"
  },
  "dependencies": {
    "three": "^0.169.0",
    "vite": "^5.4.4"
  },
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
